import React, {Component} from 'react';
import { Link } from 'react-router';

class Bucketlist extends Component {
    render() {
        return (
            <h1>This is the bucketlist page</h1>
        );
    }
}

export default Bucketlist;